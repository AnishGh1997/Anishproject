package com.dummy;

public class PrintOneToHundred {    // without using number we can print 1 to hundred
	
	public static void main(String[] args) {
		int one = 'a'/'a';  // same thing will be divided we get one as a answer number
		
		String s = "**********"; //ten times of symbols stored in this string so its length value is 10
		
		for (int i = one; i <=(s.length()*s.length()); i++) {
			
			System.out.println(i);
			
		}
		
		
		
	}

}
